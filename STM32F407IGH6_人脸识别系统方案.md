# 基于 STM32F407IGH6 的人脸识别系统方案

## 1. 项目目标

设计一套可落地的嵌入式人脸识别终端，核心目标如下：

- **本地采集 + 本地识别**：不依赖云端即可完成 1:N 人脸比对。
- **低功耗 + 快速唤醒**：适用于门禁、考勤等常电设备。
- **可扩展通信**：支持 UART/RS485/Wi-Fi（可选）上传日志与远程管理。
- **高可靠性**：具备看门狗、掉电保护、异常恢复机制。

> 说明：STM32F407IGH6（Cortex-M4，168MHz，1MB Flash，192KB SRAM）算力与内存有限，不适合在本芯片上运行大型深度网络推理。建议采用“**外置视觉前端 + MCU 业务主控**”架构。

---

## 2. 总体架构

```text
+-------------------+        +---------------------+
| 摄像头模组(带ISP) |  DCMI  | STM32F407IGH6       |
| OV2640/OV5640     +------->+ 采集调度/业务控制   |
+-------------------+        | 特征比对(轻量)      |
                             | 门禁控制/日志        |
+-------------------+  SPI   +----------+----------+
| 外部 SDRAM 8/16MB +<-------------------+
+-------------------+                     |
                                          |
+-------------------+  FSMC/SPI           |
| TFT/LCD 显示屏     +<--------------------+
+-------------------+                     |
                                          |
+-------------------+  UART/RS485/Wi-Fi   |
| 上位机/云平台      +<--------------------+
+-------------------+                     |
                                          |
+-------------------+  GPIO               |
| 门锁继电器/蜂鸣器   +<--------------------+
+-------------------+
```

### 架构建议（推荐）

- **方案 A（推荐）**：
  - 视觉前端模组（如 K210/OpenMV/专用人脸芯片）完成检测与特征提取。
  - STM32F407 负责身份管理、策略判断、I/O 控制、日志与通信。
  - 优点：识别效果好、开发周期短、功耗可控。

- **方案 B（纯 STM32 轻量识别）**：
  - STM32 完成人脸检测（Haar/LBP）+ 特征向量（简化版）。
  - 适合小容量（<100 人）且环境稳定场景。
  - 风险：误识率和鲁棒性较弱。

---

## 3. 硬件设计

## 3.1 主控与时钟

- MCU：STM32F407IGH6。
- 外部晶振：8MHz（HSE）+ RTC 32.768kHz（可选）。
- 系统时钟：168MHz。
- 启用 IWDG 独立看门狗，防止死机。

## 3.2 存储配置

- 内部 Flash（1MB）：存放 Bootloader + 主固件。
- 内部 SRAM（192KB）：运行栈、缓存、算法临时区。
- 外部存储（推荐）：
  - **SDRAM 8/16MB**：帧缓存、图像中间数据。
  - **SPI Flash 16MB+**：人脸特征库、日志、配置参数。
  - **MicroSD（可选）**：离线升级、日志导出。

## 3.3 图像采集链路

- 摄像头建议：OV2640（2MP，成本低）或 OV5640（5MP，画质更好）。
- 接口：DCMI + DMA，采集 QVGA/VGA 图像。
- 人脸识别建议分辨率：
  - 检测阶段：320×240（速度优先）。
  - 注册阶段：640×480（质量优先，可多帧融合）。

## 3.4 外设与执行器

- 显示：2.4/3.5 寸 TFT（FSMC 并口优先）。
- 音频提示：无源蜂鸣器（PWM）。
- 门锁：继电器/MOS 驱动 + 光耦隔离。
- 通信：
  - 调试串口 UART1。
  - RS485（Modbus）用于工业现场。
  - ESP8266/ESP32（AT）实现 Wi-Fi 联网。

## 3.5 电源与EMC

- 输入：12V/24V（门禁常见）→ DC-DC 降压 5V → LDO 3.3V。
- 摄像头模拟电源、数字电源分区布线。
- 关键接口加 TVS、防反接、防浪涌。
- 继电器与 MCU 地线做合理隔离回流。

---

## 4. 软件架构

## 4.1 分层设计

- **BSP 层**：时钟、GPIO、DCMI、DMA、FSMC、UART、SPI、RTC、WDT。
- **驱动层**：Camera/LCD/Flash/RS485/Wi-Fi。
- **中间件层**：文件系统（FatFs）、参数管理、日志系统。
- **算法层**：人脸检测、活体校验（可选）、特征比对。
- **应用层**：注册/识别/删除、权限管理、门禁策略、远程维护。

## 4.2 任务模型（FreeRTOS 推荐）

- `task_camera`：图像采集（高优先级）。
- `task_detect`：人脸检测与质量评分。
- `task_recognize`：特征提取 + 相似度计算。
- `task_ui`：屏幕与交互。
- `task_comm`：串口/网络协议栈。
- `task_log`：事件记录、批量写入 Flash。
- `task_watchdog`：喂狗与健康监测。

## 4.3 数据流

1. 摄像头采集帧 → DCMI DMA 写入 SDRAM。
2. 预处理（灰度化/直方图均衡/归一化）。
3. 人脸检测（ROI 裁剪 + 对齐）。
4. 特征提取（128D/256D 轻量向量）。
5. 与本地特征库计算相似度（余弦/欧氏）。
6. 满足阈值则开锁并记录日志，否则提示失败。

---

## 5. 人脸识别算法建议

## 5.1 识别流程

- **注册流程**：
  - 采集 5~10 帧高质量人脸。
  - 过滤模糊/遮挡帧。
  - 融合后生成模板向量并加密存储。

- **识别流程**：
  - 先进行人脸检测。
  - 活体检测（眨眼/头动/IR 双目，可按预算选配）。
  - 提取特征并做 1:N 匹配。
  - 输出 `用户ID + 置信度 + 时间戳`。

## 5.2 阈值策略

- `score >= T_high`：直接通过。
- `T_low <= score < T_high`：二次确认（刷卡/PIN）。
- `< T_low`：拒绝。

建议初值：`T_high = 0.82`，`T_low = 0.72`，上线后根据现场 FRR/FAR 再标定。

## 5.3 特征库存储结构（示例）

```c
typedef struct {
    uint32_t user_id;
    uint32_t timestamp;
    uint8_t  feature[128];   // 量化后的特征
    uint16_t crc;
    uint8_t  valid;
} face_template_t;
```

- 支持 A/B 双分区，避免掉电写坏。
- 每次更新采用“先写新块、后切换索引”的原子策略。

---

## 6. 安全与隐私设计

- 特征数据不存原始人脸图，仅存加密特征向量。
- 本地数据库采用 AES-128/256 加密。
- 管理员操作需二次认证（密码 + 管理卡）。
- 串口调试口量产后可锁定。
- 启用 RDP（读保护）与固件签名校验（可选）。

---

## 7. 通信协议建议

## 7.1 串口帧格式

```text
[0xAA][CMD][LEN][PAYLOAD...][CRC16]
```

常用命令：

- `0x01` 注册用户
- `0x02` 删除用户
- `0x03` 识别结果上报
- `0x04` 同步时间
- `0x05` 查询设备状态

## 7.2 日志字段

- 事件类型（注册/识别成功/识别失败/开门/告警）
- 用户 ID
- 时间戳（RTC + NTP 校时）
- 相似度分值
- 设备状态码

---

## 8. 性能指标（参考目标）

- 识别时间：`< 800ms`（单人近距正脸）。
- 首帧检测：`< 300ms`。
- 识别准确率：实验室条件 `> 98%`。
- 误识率 FAR：`< 0.1%`（需现场标定）。
- 本地库容量：
  - 轻量版：100~300 人。
  - 外置视觉前端版：1000 人以上（取决于前端芯片）。

---

## 9. 开发里程碑

- **M1（第1~2周）**：硬件连通（摄像头、LCD、存储、串口）。
- **M2（第3~4周）**：完成图像采集 + UI + 基础通信协议。
- **M3（第5~6周）**：接入识别算法，打通注册/识别/日志闭环。
- **M4（第7~8周）**：稳定性测试（72h 老化、掉电恢复、看门狗）。
- **M5（第9周）**：现场试点与阈值标定。

---

## 10. 风险与规避

- **算力不足**：采用外置 AI 前端，STM32 只做控制与策略。
- **内存瓶颈**：使用 SDRAM + 分块处理 + 定点量化。
- **复杂光照误检**：补光灯 + 自动曝光 + 多帧融合。
- **口罩/侧脸场景**：多模态认证（刷卡/PIN）兜底。
- **数据安全风险**：加密存储 + 权限分级 + 审计日志。

---

## 11. 推荐 BOM（示例）

- STM32F407IGH6 主控板
- OV2640 摄像头模组
- 8MB/16MB SDRAM
- 16MB SPI NOR Flash
- 2.8 寸 TFT LCD（ILI9341/并口）
- RS485 收发器（MAX3485）
- Wi-Fi 模组（ESP8266，可选）
- 继电器 + 驱动电路 + 光耦
- 蜂鸣器、补光 LED、按键

---

## 12. 结论

基于 STM32F407IGH6 可以实现一套工程可落地的人脸识别终端。最佳实践是将“高算力视觉算法”下沉到外置前端，STM32 负责业务控制与系统可靠性，从而在成本、性能、开发周期之间取得平衡。
